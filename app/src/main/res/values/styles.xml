<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/topBar</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/topBar</item>
        <!-- background-->
        <item name="android:windowBackground">@color/backGround</item>
    </style>

    <style name="PlayToggleStyle" parent="android:Widget">
        <item name="android:background">@drawable/ic_play_toggle_bg</item>
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>
    </style>
    <style name="RepeatToggleStyle" parent="android:Widget">
        <item name="android:background">@drawable/ic_repeat_toggle_bg</item>
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>
    </style>
    <style name="playToggleButton" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:buttonStyleToggle">@style/PlayToggleStyle</item>
        <item name="android:textOn"></item>
        <item name="android:textOff"></item>
    </style>
    <style name="repeatToggleButton" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:buttonStyleToggle">@style/RepeatToggleStyle</item>
        <item name="android:textOn"></item>
        <item name="android:textOff"></item>
    </style>
</resources>
